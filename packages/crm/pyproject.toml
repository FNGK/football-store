[project]
name = "amp-crm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["google-cloud-bigquery>=3.27.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["amp_crm"]
